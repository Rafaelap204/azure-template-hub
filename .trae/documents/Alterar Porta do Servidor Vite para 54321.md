Vou encerrar a aplicação atual que está rodando na porta 8080 e iniciar uma nova na porta 54321, conforme solicitado.

**Plano de Execução:**

1.  **Parar o processo atual:** Encerrar o servidor Vite que está rodando no terminal.
2.  **Configurar a nova porta:** Modificar o arquivo `vite.config.ts` para alterar a porta de `8080` para `54321`.
3.  **Iniciar o servidor:** Rodar novamente o comando `npm run dev` para subir a aplicação na nova porta.
4.  **Abrir o preview:** Utilizar a ferramenta `OpenPreview` para exibir a aplicação rodando em `http://localhost:54321`.

**Arquivo a ser modificado:**
-   [vite.config.ts](file:///C:/Users/Gestalt/Desktop/agentectu1/azure-template-hub/vite.config.ts)

Gostaria de confirmar a execução deste plano?